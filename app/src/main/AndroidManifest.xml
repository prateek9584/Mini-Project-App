<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prateek.Miniproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Otp" />
        <activity android:name=".Confirm" />
        <activity
            android:name=".ownerActivity"
            android:label="@string/title_activity_owner"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.example.prateek.fragment.Prem"
            android:label="@string/title_activity_prem"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>